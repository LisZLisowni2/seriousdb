# Configuration

SeriousDB can be configured using environment variables or .env file in the root directory.

| Variable | Description | Default |
| :--- | :--- | :--- |
| `DB_FILE` | Path to the database storage file | `.sdb` |

### Environment File

You can copy `.env.example` to `.env` and adjust the settings:

```bash
cp .env.example .env
```
